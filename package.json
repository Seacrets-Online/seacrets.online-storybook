{
  "name": "@seacrets/design-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tokens:build": "node scripts/build-tokens.js",
    "predev": "npm run tokens:build",
    "dev": "storybook dev -p 6006",
    "prebuild": "npm run tokens:build",
    "build": "storybook build",
    "prestorybook": "npm run tokens:build",
    "storybook": "storybook dev -p 6006",
    "prebuild-storybook": "npm run tokens:build",
    "build-storybook": "storybook build",
    "validate": "npm run tokens:build && vite build --mode development",
    "chromatic": "dotenv -e .env -- chromatic --exit-zero-on-changes"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@storybook/addon-a11y": "^10.2.7",
    "@storybook/addon-docs": "^10.2.7",
    "@storybook/react-vite": "^10.2.7",
    "@vitejs/plugin-react": "^5.1.3",
    "chromatic": "^15.0.0",
    "dotenv-cli": "^11.0.0",
    "storybook": "^10.2.7",
    "style-dictionary": "^5.2.0",
    "vite": "^7.3.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
