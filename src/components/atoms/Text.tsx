import { Typography as MuiTypography } from '@mui/material';
import type { TypographyProps } from '@mui/material';

export const Text = (props: TypographyProps) => <MuiTypography {...props} />;

export default Text;
