import { ArrowBack } from '@mui/icons-material';
import type { SvgIconProps } from '@mui/material';

export const ArrowBackIcon = (props: SvgIconProps) => <ArrowBack {...props} />;

export default ArrowBackIcon;
