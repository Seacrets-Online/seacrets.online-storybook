import { InboxOutlined } from '@mui/icons-material';
import type { SvgIconProps } from '@mui/material';

export const InboxOutlinedIcon = (props: SvgIconProps) => <InboxOutlined {...props} />;

export default InboxOutlinedIcon;
