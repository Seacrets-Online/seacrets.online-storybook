import { MoreHoriz } from '@mui/icons-material';
import type { SvgIconProps } from '@mui/material';

export const MoreHorizIcon = (props: SvgIconProps) => <MoreHoriz {...props} />;

export default MoreHorizIcon;
