import { MonetizationOn } from '@mui/icons-material';
import type { SvgIconProps } from '@mui/material';

export const MonetizationOnIcon = (props: SvgIconProps) => <MonetizationOn {...props} />;

export default MonetizationOnIcon;
